# Core dependencies
python-dotenv>=1.0.0
requests>=2.28.0

# For NVIDIA SANA and PixArt generation (API-based models via Replicate)
replicate>=0.20.0

# For DALL-E 2 generation (API-based model via OpenAI)
openai>=1.0.0

# For Stable Diffusion generation (local model inference)
# Note: Install torch with CUDA support for GPU acceleration:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
diffusers>=0.21.0
transformers>=4.30.0
huggingface-hub>=0.17.0

